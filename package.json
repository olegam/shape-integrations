{
  "name": "integrations",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "lint": "prettier --no-semi --single-quote --write index.js projects/**/*.js",
    "test": "LOCAL=true mocha  --reporter mocha-better-spec-reporter --timeout 10000 tests/*.js"
  },
  "author": "Ole Gammelgaard Poulsen",
  "license": "MIT",
  "dependencies": {
    "aws-sdk": "^2.50.0"
  },
  "devDependencies": {
    "ajv": "^5.1.3",
    "lambda-local": "^1.4.2",
    "mocha": "^3.4.1",
    "mocha-better-spec-reporter": "^3.1.0",
    "prettier": "^1.3.1",
    "superagent": "^3.5.2"
  }
}
